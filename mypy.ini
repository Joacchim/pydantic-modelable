[mypy]
# The mypy-plugin tests are exercised by the `plugin` tox env (which runs mypy
# with the plugin enabled); their fixtures intentionally rely on the plugin, so
# they are excluded from the plain `mypy --strict` typecheck of the library.
exclude = tests/mypy_plugin/

[mypy-aenum]
ignore_missing_imports = True
